<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: Compose drives most styling, so a Material 3 splash-friendly base is enough. -->
    <style name="Theme.StockWidget" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
